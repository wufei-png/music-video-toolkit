[project]
name = "mvt-separation-runtime"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "audioread>=3.0,<4",
  "audio-separator[cpu]==0.44.2",
  "librosa==0.10.2.post1",
]

[tool.uv]
package = false
